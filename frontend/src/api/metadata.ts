import { AddAttachment, CreateProof, ListAttachments, ListProofs, RemoveAttachment, UpdateProofStatus } from '../../wailsjs/go/main/App'
export interface AttachmentRecord { id:string; ownerType:string; ownerId:string; fileName:string; path:string; mimeType:string; sizeBytes:number|null; checksum:string; category:string; notes:string; createdAt:string }
export interface ProofRecord { id:string; ownerType:string; ownerId:string; attachmentId:string; status:string; versionLabel:string; approverNote:string; internalNote:string; preparedAt:string|null; approvedAt:string|null; rejectedAt:string|null; createdAt:string|null }
export const metadataApi={
 attachments(ownerType:string,ownerId:string):Promise<AttachmentRecord[]>{return ListAttachments(ownerType,ownerId) as unknown as Promise<AttachmentRecord[]>}, addAttachment(input:{ownerType:string;ownerId:string;fileName:string;path:string;mimeType:string;sizeBytes:number|null;checksum:string;category:string;notes:string}):Promise<AttachmentRecord>{return AddAttachment(input.ownerType,input.ownerId,input.fileName,input.path,input.mimeType,input.sizeBytes,input.checksum,input.category,input.notes) as unknown as Promise<AttachmentRecord>}, removeAttachment(id:string):Promise<void>{return RemoveAttachment(id) as unknown as Promise<void>}, proofs(ownerType:string,ownerId:string):Promise<ProofRecord[]>{return ListProofs(ownerType,ownerId) as unknown as Promise<ProofRecord[]>}, createProof(input:{ownerType:string;ownerId:string;attachmentId:string;status:string;versionLabel:string;approverNote:string;internalNote:string}):Promise<ProofRecord>{return CreateProof(input.ownerType,input.ownerId,input.attachmentId,input.status,input.versionLabel,input.approverNote,input.internalNote) as unknown as Promise<ProofRecord>}, transitionProof(ownerType:string,ownerId:string,proofId:string,status:string,approverNote:string):Promise<ProofRecord>{return UpdateProofStatus(ownerType,ownerId,proofId,status,approverNote) as unknown as Promise<ProofRecord>},
}
